{
  "name": "cf-mcp-hub",
  "version": "1.0.0",
  "description": "Cloudflare Remote MCP hub — token-saving tools for coding and marketing, accessible from any Claude session",
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "deploy": "wrangler deploy src/index.ts",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240924.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.80.0"
  },
  "dependencies": {
    "agents": "^0.0.96"
  }
}
